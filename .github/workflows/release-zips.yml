# Один релиз = все ZIP + versions.json с версиями утилит.
# Триггер: gh release create v1.0.003 ...
# Панели проверяют updates через versions.json в assets.

name: Release ZIPs

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  zip-and-upload:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.tag_name }}

      - name: Extract versions and create versions.json
        run: |
          mkdir -p out
          entries=""
          for dir in AETimeTracker SmartSyncAssetsCEP yandex-map-cleaner; do
            if [[ ! -d "$dir" ]]; then continue; fi
            ver="0.0.0"
            if [[ -f "$dir/CSXS/manifest.xml" ]]; then
              ver=$(grep -oE 'ExtensionBundleVersion="[^"]+' "$dir/CSXS/manifest.xml" | cut -d'"' -f2 | head -1)
            fi
            if [[ -z "$ver" || "$ver" == "0.0.0" ]] && [[ -f "$dir/manifest.json" ]]; then
              ver=$(grep -oE '"version"[[:space:]]*:[[:space:]]*"[^"]+' "$dir/manifest.json" | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | head -1)
            fi
            [[ -z "$ver" ]] && ver="0.0.0"
            [[ -n "$entries" ]] && entries="$entries,"
            entries="$entries\"$dir\":\"$ver\""
          done
          echo "{$entries}" > out/versions.json
          cat out/versions.json

      - name: Create ZIPs and upload
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ github.event.release.tag_name }}
        run: |
          for d in */; do
            name="${d%/}"
            [[ "$name" == .* ]] && continue
            [[ "$name" == "node_modules" ]] && continue
            [[ "$name" == "out" ]] && continue
            zip -r "${name}.zip" "$name"
          done
          cp out/versions.json .
          gh release upload "$TAG" *.zip versions.json --clobber

      - name: Push versions.json to main (for raw.githubusercontent.com)
        env:
          TAG: ${{ github.event.release.tag_name }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git checkout main
          cp out/versions.json .
          git add versions.json
          git diff --staged --quiet || git commit -m "chore: update versions.json from release $TAG"
          git push origin main || true
